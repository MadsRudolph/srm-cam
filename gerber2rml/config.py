"""Job/Tool parameter dataclasses and SRM-20 defaults.

Defines the plain-data structures passed between the loader, engine and machine
backends (bit diameter, cut depths, feeds, offsets, tabs, mirror, board
thickness). To be filled in during implementation; see ``docs/design.md`` §4 for
the full variable table and SRM-20 default values.
"""
