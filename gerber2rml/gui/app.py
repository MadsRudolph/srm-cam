"""GUI entry point.

Builds the PySide6 window (file loader, machine dropdown, per-operation
parameter tabs, matplotlib toolpath preview, export button). Stub — see
``docs/design.md`` §5.
"""


def main() -> int:
    """Launch the gerber2rml desktop app."""
    raise NotImplementedError("GUI not yet implemented; see docs/design.md")


if __name__ == "__main__":
    raise SystemExit(main())
