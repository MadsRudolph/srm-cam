# LOOSE declaration of the build environment — the ".in" file.
#
# Do NOT install from this directly for a release build: use
# packaging/requirements-lock.txt, which pins every version (including
# transitives) to the set that produced the shipped installer. This file
# exists to say what we actually depend on, and to regenerate the lock when
# we deliberately upgrade something (see the header of the lock file).
#
# build.ps1 creates an isolated venv from the LOCK so the installer bundles
# only what the app needs — building from a fat conda base drags in
# torch/scipy/pygame and balloons the bundle to multiple GB.
gerbonara>=1.0
shapely>=2.0
PySide6>=6.5
matplotlib>=3.7
pyqtgraph>=0.13
PyOpenGL>=3.1
pyserial>=3.5
qrcode>=7.0
pyinstaller>=6.0
